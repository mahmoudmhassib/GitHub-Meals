import 'package:flutter/material.dart';
class FavouriteItem extends StatelessWidget {
 @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(' no select fav , '),
      ),
    );
  }
}
